Surface container on the navy canvas.

```jsx
<Card>Standard panel</Card>
<Card accent elevated>Copper-ruled callout with shadow</Card>
```

`elevated` → stronger shadow. `accent` → copper left rule + squared left corners (the "mirror" callout look). `padding` overrides the default.
