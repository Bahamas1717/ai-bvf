CI pre-flight gate stamp — the PASS / FAIL result of `aibvf-check`.

```jsx
<GateBadge status="pass" size="lg" />
<GateBadge status="fail" />
```

`status`: `pass` (green) · `fail` (red). Monospace, verdict-coloured, glowing square dot. Used standalone or inside `Scorecard`.
