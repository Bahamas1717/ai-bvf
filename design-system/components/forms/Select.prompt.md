Dropdown on navy with a copper chevron.

```jsx
<Select label="Readiness" options={['traditional','emerging','optimised']} />
<Select label="AI tier" options={[{value:'gen2',label:'Gen 2 · Assistive'}]} />
```

`options` accepts strings or `{value,label}`. Copper focus ring, native select semantics.
