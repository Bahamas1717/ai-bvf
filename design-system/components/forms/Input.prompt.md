Text field on navy with copper focus ring.

```jsx
<Input label="Revenue (EUR)" placeholder="400,000,000" hint="Annual, most recent FY" />
```

Optional `label` (uppercase copper) and `hint`. Passes through all native input props.
